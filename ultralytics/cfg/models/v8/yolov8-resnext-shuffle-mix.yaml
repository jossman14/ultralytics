# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license
# YOLOv8-ResNeXt-Shuffle-Mix
# Mixed backbone: C2fResNeXt (cardinality-grouped convolutions) at P2/P3 for high-capacity
# early features, switching to C2fShuffle (channel-shuffle units) at P4/P5 for cheap deep
# feature mixing. A NonLocalBlock is inserted right before SPPF for global context, and
# SEBlock gates every detection scale for channel recalibration.
nc: 80 # number of classes
scales: # model compound scaling constants
  # [depth, width, max_channels]
  n: [0.33, 0.25, 1024]
  s: [0.33, 0.50, 1024]
  m: [0.67, 0.75, 768]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.25, 512]

backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2 (stem)
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 3, C2fResNeXt, [128, True]]
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 6, C2fResNeXt, [256, True]] # 4 (P3)
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 6, C2fShuffle, [512, True]] # 6 (P4)
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 3, C2fShuffle, [1024, True]]
  - [-1, 1, NonLocalBlock, []] # 9 - global context before SPPF
  - [-1, 1, SPPF, [1024, 5]] # 10

head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 6], 1, Concat, [1]] # 12 cat backbone P4
  - [-1, 3, C2f, [512]]
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 4], 1, Concat, [1]] # 15 cat backbone P3
  - [-1, 3, C2f, [256]] # 16 (P3/8-small)
  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 13], 1, Concat, [1]] # 18 cat head P4
  - [-1, 3, C2f, [512]] # 19 (P4/16-medium)
  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 10], 1, Concat, [1]] # 21 cat head P5
  - [-1, 3, C2f, [1024]] # 22 (P5/32-large)
  - [16, 1, SEBlock, []] # 23 SE on P3
  - [19, 1, SEBlock, []] # 24 SE on P4
  - [22, 1, SEBlock, []] # 25 SE on P5
  - [[23, 24, 25], 1, Detect, [nc]] # 26 Detect(P3, P4, P5)
